name: flutter_update_version
description: Flutter Update Version package.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^1.0.0
  http: ^0.13.1
  html: ^0.15.0
  url_launcher: ^6.0.2
  collection: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
